{
  "name": "ansh-ebook-root",
  "version": "1.0.1",
  "comment": "Triggering fresh build due to previous timeout",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix server --no-audit --no-fund && npm install --prefix frontend --no-audit --no-fund",
    "build": "npm run build --prefix frontend",
    "render-build": "npm install --prefix server --no-audit --no-fund && npm install --prefix frontend --no-audit --no-fund && npm run build --prefix frontend",
    "start": "node server/index.js"
  }
}
